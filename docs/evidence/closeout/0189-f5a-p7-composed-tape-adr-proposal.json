{
    "schema_version": "AdrProposal.v1",
    "slice_id": "F5a / P7 Packet 7 / bounded synthetic composed-tape verification (P7 closure gate)",
    "base_main": "56628c5",
    "worktree": "/home/russell/wt/f5a-p7-remainder-20260916",
    "branch": "codex/f5a-p7-remainder-20260916",
    "adr": "ADR-0146 - bounded synthetic composed-tape verification (P7 closure gate)",
    "task": "DESIGN-ONLY packet: propose the ADR closing P7's final remaining gate (composed-tape verification), per RE-ENTRY.md's own stated next-gate ordering. No production code, tests, or RED/GREEN were run; no merge occurred.",
    "proposal": "Re-verifies the 2026-09-16 architecture gate (evidence 0176: a ReplayRun consumer needed to both read an outer-manifest root via legacy v1 open_capture/VerifiedCapture, single-CAPTURED, AND be a second consumer of the data root via P4 commit_p4_batch multi-consumer, which run-identity exclusivity then forbade mixing in one run) against the CURRENT dskit/pipeline/trust.py, not by assumption. Finds gate 0176 resolvable by direct reading: (1) ADR-0129's accessor (CapturedAuthorizationRecord.read_member_bytes / .lifecycle_captured_receipt_sha256, trust.py:4824-4898, backed by _LifecycleAuthorizationLedger.read_p4_member/p4_receipt_digest, trust.py:5305-5343) is fully merged and applies to any committed P4 capture, fixed or dynamic; (2) ADR-0143/0144's dynamic authority already reaches a real authorize_capture_set -> CAPTURED admission end-to-end, exercised by passing tests (tests/pipeline/test_captured_authorization.py: _adr143_issued_case, _adr144_document_and_captures, _adr144_admission_ref, test_adr144_dynamic_authorize_capture_set_reaches_genuine_captured_admission); (3) run-identity exclusivity (commit_legacy_capture trust.py:5177, commit_p4_batch trust.py:5437) is a per-ledger-instance invariant, not process-global -- each authority instance owns its own _LifecycleAuthorizationLedger (ADR-0143 Decision point 3), and ADR-0144's own Non-goals explicitly disclaim any cross-instance run-identity check. ADR-0146 adopts the smaller of two independently sufficient resolutions: applying ADR-0129's accessor to a SINGLE dynamic-authority CAPTURED admission supplies both member bytes and a receipt digest from the SAME capture, eliminating the need for a second (legacy) capture leg entirely -- so there is nothing left to mix, irrespective of the per-ledger argument. The alternative the task explicitly asked to be considered (mixing the dynamic authority for one leg and legacy v1 for a separately-published outer-manifest leg) is recorded as also conflict-free by the per-ledger argument, but NOT adopted, because it would require a new, unbuilt capability -- publishing a brand-new root outside the fixed P4 corpus and outside the dynamic authority's closed 12-reference graph (ADR-0142) -- reopening exactly the kind of unbounded scope that produced three failed whole-composition Phase 0 cycles on the original ADR-0127 Decision.2 design (evidence 0179). ADR-0146 therefore formally narrows ADR-0127 Decision.2: the 'outer manifest' (CapturedReplayTape.v1) is constructed and default-deny verified IN the consuming process from bytes resolved out of the same data capture, never itself republished as a second captured root; ReplayTapeManifestProducer/ReplayTapeManifestCapture as separately-published entities (never implemented in code -- confirmed by direct search) are retired from the plan. The proposed design reuses ONLY already-merged, already-tested symbols (CapturedAuthorizationRecord's ADR-0129 accessor, the dynamic authority's ADR-0143/0144 fixtures, CapturedReplayTape._build/canonical_bytes/parse and verify_causal_order from ADR-0145, and its existing envelope-shape fixture builder in tests/production/test_event_envelope_causal_order.py) and adds NO new function or class to trust.py or production/bundles.py -- the entire deliverable is new fixture-only integration test code (recommended placement: appended to tests/pipeline/test_captured_authorization.py, a Phase 0 pin) proving one straight-line synthetic tape: one dynamic-authority CAPTURED admission -> resolved roster + raw-event member bytes -> projected dskit.event-envelope/v2 objects -> a built-and-reparsed CapturedReplayTape -> verify_causal_order, plus at least one negative-family case proving the checks are live against resolved, not fixture-literal, bytes.",
    "scope": "Design proposal only: ADR-0146 appended to docs/architecture/decision-log.md and this evidence file. No production code, pipeline code, or test file was written or edited. No RED/GREEN. No merge.",
    "non_goals": "Wiring ReplayRun.run() into the pipeline execution engine (a distinct, larger capability -- delivering a live CapturedAuthorizationRecord/session to any node's run(ctx, inputs) -- that exists for no node today, not only ReplayRun); a second manifest-publish capability or any extension to the dynamic authority's closed 12-reference graph; exercising ADR-0128 multi-consumer capture as a requirement (available, not required); resurrecting ADR-0130's undelivered SourceRosterCapture.v1/RawEventDatasetCapture.v1/DatasetCaptureAuthorization.v1 broker; the full master F3 EventEnvelope.v2 lane; real replay, backtest, paper or live operation; Packet 8 durable consume-once (F5A-R23); deployment. Any edit to trust.py's dynamic P4 authority, the five ADR-0143 forbidden-legacy symbols, or ADR-0144's dynamic-dispatch code.",
    "gate_0176_resolution": {
        "original_blocker": "evidence 0176: ReplayRun needed both a v1 open_capture/VerifiedCapture read of an outer manifest (single-CAPTURED) and to be a second P4 consumer of the data root (multi-consumer, but at the time an empty CapturedAuthorizationRecord with no member-read/receipt seam); run-identity exclusivity then forbade mixing a legacy and a P4 capture in one run.",
        "still_blocked_or_resolved": "resolved -- re-verified against current trust.py, not assumed",
        "adopted_resolution": "eliminate the second (legacy) capture leg: ADR-0129's accessor, applied to one dynamic-authority CAPTURED admission (ADR-0143/0144), already supplies both member bytes and a receipt digest from the same capture; no legacy v1 capture occurs anywhere in the design, so there is no second leg to mix with the P4 leg.",
        "alternative_considered": "mixing the dynamic authority for the data leg with a separate legacy-v1-captured outer-manifest leg (the task's suggested primary option) -- confirmed also conflict-free, since run-identity exclusivity is enforced per-ledger-instance (ADR-0143 Decision point 3, ADR-0144 Non-goals) and the dynamic authority's ledger is a distinct instance from any other broker's; NOT adopted because it requires a new, unbuilt capability (publishing a brand-new root outside the fixed corpus and the dynamic authority's closed 12-reference graph) that this ADR's adopted design does not need.",
        "code_evidence_cited": [
            "trust.py:4824-4898 CapturedAuthorizationRecord.read_member_bytes/.lifecycle_captured_receipt_sha256",
            "trust.py:5305-5343 _LifecycleAuthorizationLedger.p4_receipt_digest/.read_p4_member",
            "trust.py:5177 commit_legacy_capture's 'legacy capture cannot reuse a P4 execution run' (checks self._p4_entries() only)",
            "trust.py:5437 commit_p4_batch's 'P4 capture cannot reuse a legacy execution run' (checks self._legacy_captures() only)",
            "tests/pipeline/test_captured_authorization.py: _adr143_issued_case, _adr144_document_and_captures, _adr144_admission_ref, test_adr144_dynamic_authorize_capture_set_reaches_genuine_captured_admission (passing today)",
            "ADR-0142 Decision point 3 (dynamic graph resolve() closed to exactly 12 references)",
            "ADR-0144 Non-goals ('cross-instance run-identity checks between the legacy and dynamic authorities... deliberate non-goal, unchanged')"
        ]
    },
    "open_question_for_reviewer": "Whether Decision point 9's recommendation -- appending the composed-tape proof (now a call to compose_replay_tape) to tests/pipeline/test_captured_authorization.py rather than a new tests/production/test_composed_replay_tape.py -- is the right placement given that file's already-large size (5479 lines before this addition), or whether Phase 0 should instead factor _adr143_issued_case/_adr144_document_and_captures/_adr144_admission_ref into a shared, importable fixture module first so a smaller, dedicated production-side test file can reuse them without growing test_captured_authorization.py further.",
    "deployment_eligible": false,
    "tests_run": "none; design review only",
    "code_changed": "none; docs/architecture/decision-log.md (ADR-0146 Decision points redesigned in place, Scope decision subsection added) and this evidence file only. No dskit/ production code, pipeline code, or test file was written or edited by this correction round -- only the ADR TEXT now specifies a production function (compose_replay_tape) that Phase 0 will implement later.",
    "review_status": "Independent preapproval review (round 1) was conducted against the original draft (evidence unchanged above through 'gate_0176_resolution') and found 1 Critical, 2 Major, 1 Minor, 2 Nit; see adr0146_preapproval_review below for the findings and this round's corrections. This corrected draft awaits owner ADR approval, then a fresh independent preapproval recheck and Phase 0 skeptic review before any RED/GREEN, following docs/skills/implementation-workflow.md and docs/skills/skeptic-review.md.",
    "adr0146_preapproval_review": {
        "reviewer": "independent, fresh-context adversarial skeptic (preapproval round 1)",
        "result": "1 Critical, 2 Major, 1 Minor, 2 Nit, plus 2 claims independently verified correct and requiring no change",
        "critical": "test-only proof is likely under-scoped: RE-ENTRY.md's stated gate is 'constructing a runtime composed-tape capability that calls verify_causal_order end-to-end' -- language implying a CALLABLE CAPABILITY, not merely a passing test. Owner + Phase 0 must explicitly approve whether test-only proof is sufficient P7 closure, or whether a thin wrapper function marshaling the pieces is the true minimum closure.",
        "major_1": "the accessor's dynamic-authority compatibility was claimed 'by inspection' (re-reading trust.py) but the original ADR did not require a RED test that actually exercises read_member_bytes/lifecycle_captured_receipt_sha256 against a DYNAMIC-authority-issued record specifically -- all existing tests use the fixed authority, and the ADR's own adopted design (Decision point 2) names the dynamic authority as its positive proof case, so this was the ADR's own claim going untested by its own RED matrix.",
        "major_2": "the three-hop (data capture -> separate manifest-producer consumer -> manifest publish -> third ReplayRun consumer) original ADR-0127 Decision.2 design is collapsed to a one-hop (single dynamic-authority capture, in-process manifest construction, no separate publish/consumer hop) proof; this was disclosed in the 'Correction to ADR-0127 Decision.2' paragraph but not recorded as an explicit, clearly labeled owner scope-narrowing decision, unlike this ADR lineage's own established pattern (e.g. evidence 0179's owner_delegated_scope_decision entries).",
        "minor": "fixture-builder duplication risk: the original Decision point 5 reused tests/production/test_event_envelope_causal_order.py's test-private _envelope/_bytes fixture builder for envelope construction, while the composed-tape test would also need its own construction/orchestration code -- two places building the same dskit.event-envelope/v2 shape.",
        "nit_1": "the new composed-tape fixture's roster schema string (dskit.composed-tape-roster-fixture/v1) needed a one-line comment marking it test-only/placeholder, not a real SourceRosterCapture.v1.",
        "nit_2": "the rejected two-broker alternative's dismissal reasoning could be tightened (optional, reviewer's own disposition; not applied this round -- the existing 'Considered and recorded, not adopted' paragraph above is left as originally written, since the reviewer flagged this as skippable and no clean tightening presented itself without re-litigating claim (e), which the reviewer separately found sound).",
        "verified_no_change_needed": [
            "claim (c) run-identity exclusivity is genuinely per-ledger-instance, no shared/global state",
            "claim (e)'s core reasoning for rejecting the two-broker alternative is correct (just could be tightened, optional -- see Nit 2 disposition above)"
        ],
        "corrections": "Critical: closed by redesigning (not merely re-justifying) the ADR to add a real, small, reusable, pure production function, compose_replay_tape, in dskit/production/bundles.py (new Decision point 7) -- the actual 'composed-tape capability' P7's gate names. It resolves member bytes via the ADR-0129 accessor, projects raw-event/v1 members into event-envelope/v2 objects via the same validated path verify_causal_order itself uses (_check_event_envelope, already in bundles.py and not test-private), assembles a CapturedReplayTape.v1, and calls verify_causal_order, returning the verified tape on success and raising on any violation. The Decision point 9 test now calls this function rather than orchestrating inline. Major 1: closed by new Decision point 8, an explicit Phase 0 RED-matrix requirement driving the full chain through the dynamic authority (trust._development_dynamic_p4_broker / trust._DynamicP4TrustedArtifactResolver) end-to-end through compose_replay_tape, stated as this ADR's own positive proof case that must not default to only the fixed-authority path. Major 2: closed by a new, clearly labeled 'Scope decision: one-hop vs three-hop composition' subsection (placed after the existing 'Correction to ADR-0127 Decision.2' paragraph), stating in plain third-person process language -- no first- or second-person owner-authorization quoting in decision-log.md itself, per this session's harness classifier precedent (ADR-0143's Non-goals/Status sections) -- that the three-hop design was abandoned after three failed Phase 0 cycles (evidence 0174/0175/0176) because production republishing a second manifest root outside the fixed P4 corpus and the dynamic authority's closed graph is unbuilt capability, that this ADR proves the achievable one-hop version as P7's actual, honest closure criterion for a bounded nondeployment synthetic slice, and that a real multi-hop composition remains explicitly out of scope, ordered under the real F3 lane's own future work. Minor: closed as a consequence of the Critical's fix -- compose_replay_tape is now the single owner of envelope-construction logic; the test calls it instead of reusing or re-deriving the test-private _envelope/_bytes builder, and the ADR's 'What is already merged and reused as-is' list was corrected to say so explicitly. Nit 1: closed -- Decision point 3 now carries an explicit 'Implementation pin (Nit, closed here)' requiring a one-line placeholder/test-only comment on the schema string wherever it is defined in production/bundles.py. Nit 2: left as originally written (optional per the reviewer's own disposition; no clean tightening found without re-litigating the already-sound claim (e)). The ADR's Non-goals section was also updated to replace any 'no production code' framing with the precise remaining non-goal -- a real multi-hop, three-consumer composition chain -- since compose_replay_tape is now real, bounded production surface this ADR itself adds.",
        "tests_run": "none; design review",
        "disposition": "1 Critical + 2 Major + 1 Minor corrected by design changes (not textual-only); 1 Nit corrected, 1 Nit left open by the reviewer's own optional disposition. Per docs/skills/skeptic-review.md's severity-proportionate process, a Critical/Major correction round requires a fresh independent recheck before owner approval -- see recheck below.",
        "recheck": {
            "reviewer": "independent, fresh-context adversarial skeptic (preapproval recheck round)",
            "result": "0 Critical, 0 Major, 1 new Minor, 0 Nit. READY_FOR_OWNER_APPROVAL: yes.",
            "corrections_verified": [
                "compose_replay_tape's signature and 10-step body are field-level precise (roster resolution, source_rank_policy_sha256, per-member envelope assembly via _check_event_envelope, internally-derived data_capture_root, receipt via record.lifecycle_captured_receipt_sha256, build/canonical_bytes/parse round-trip, verify_causal_order call) -- a Phase 0 implementer has no load-bearing ambiguity",
                "Decision point 8's dynamic-authority RED requirement confirmed explicit and non-defaulting",
                "the 'Scope decision: one-hop vs three-hop composition' subsection confirmed present and unambiguous",
                "duplication risk confirmed resolved: envelope construction has exactly one owner (compose_replay_tape), the test calls it rather than re-deriving",
                "confirmed no forbidden-symbol reach and no dskit.pipeline.trust import from bundles.py -- layering boundary preserved"
            ],
            "new_minor": "compose_replay_tape is a genuinely callable, reusable, clean-signature production function; without an explicit docstring warning, a future caller unaware of this ADR's nondeployment scope could mistake it for a real replay capability. Recommends a one-line docstring warning.",
            "tests_run": "none; design review"
        }
    },
    "owner_decision": "2026-09-16: orchestrating session (Claude Sonnet 5), acting under the user's explicit standing authorization to draft, critique and approve ADRs autonomously for this task, approves ADR-0146 following a clean independent preapproval review, a corrected redesign closing 1 Critical + 2 Major + 1 Minor, and a clean independent recheck (0 Critical/Major, 1 new Minor closed inline as a docstring-warning implementation pin in decision-log.md Decision point 7). ADR-0146 status changed to accepted. Phase 0 skeptic review is required next, per docs/skills/skeptic-review.md, before any RED -- this is P7's final closure gate.",
    "gate": "Preapproval review, correction, and recheck complete (0 Critical/Major after correction; 1 Minor closed inline). Owner approval recorded above. Phase 0 skeptic review required next, before any RED/GREEN. No Phase 0, no RED/GREEN, and no merge have occurred."
}
