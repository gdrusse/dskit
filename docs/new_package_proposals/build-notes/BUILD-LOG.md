# Build log — dskit.production (autonomous run, started 2026-09-05)

Branch: claude/dskit-production-build-3g17vw (off origin/main @ 03d797c)
Brief for agents: scratchpad/BRIEF.md. Plan: docs/new_package_proposals/production.md.
Baseline: full suite green on main (see below); ruff had 1 pre-existing D103 in check_plan.py (fixed).

## Group status (T=tests by Opus, F=first build by Fable, R1=Opus review, Rn=Sonnet review)
| group | modules | T | F | R1 | fix | R2 | committed |
|---|---|---|---|---|---|---|---|
| G1 | vocab base redact records (+test_purity) | | | | | | |
| G2 | document release | | | | | | |
| G3 | clock sessions cadence | | | | | | |
| G4 | ledger state | | | | | | |
| G5 | resilience metrics | | | | | | |
| G6 | control accounting | | | | | | |
| G7 | guards | | | | | | |
| G8 | breaker arming coordination | | | | | | |
| G9 | ids bundles policy | | | | | | |
| G10 | pipeline seam (SubgraphRunner, ExecutionPolicy, serving_effect, serving_contract, audit) | | | | | | |
| G11 | feed decider (+conftest synthetic run) | | | | | | |
| G12 | reconcile readiness | | | | | | |
| G13 | verifier executor | | | | | | |
| G14 | monitors | | | | | | |
| G15 | alerts health | | | | | | |
| G16 | leg | | | | | | |
| G17 | compose loop | | | | | | |
| G18 | __main__ + e2e | | | | | | |
| G19 | cross-cutting: test_oop test_producers | | | | | | |
| G20 | docs examples skeleton | | | | | | |
| S0 | Stage 0: phase-2 spec in plan + skeptic review | | | | | | |

## Plan gaps found (category, section, resolution)
- [bookkeeping] §8 test_vocab.py "scans §5–§7" contradicts §5.16 "a shipped test must not depend on a proposal file": completeness scan moves to check_plan.py; test_vocab pins an independent restated list. (to apply)

## Decisions / conventions beyond the plan
- Registry API, ProductionError shape, canonical_bytes rules: see BRIEF.md.
- [bookkeeping] §9.1 API block: `SubgraphRunner` gains `run_keys(keys, outputs, ctx, prev_bindings)` (the base-pass verb) and `apply_param_override` becomes public — see SEAM-DESIGN.md §3. Needs skeptic review with the other plan edits.
- Baseline: full suite green after installing numpy/sklearn/pyarrow/optuna/pyomo/highspy (3407 passed pre-pyomo; pyomo 82 passed after solver). check_plan.py D103 fixed.

## Wave 1 launched (5 Opus test authors): T1 G1, T2 G2, T3 G3, T4 G4(ledger), T5 G5(state).

## T4 ledger tests DONE (101 tests, red for the right reason). Decisions pinned by T4 (implementer must follow):
- `JsonlLedger(serve_root, process_id, release_hash, *, clock, fsync="every", rotate=None, state=None, snapshot_every=None)`; series_id comes from serve_root.
- `ServeRoot(root, series_id)` accessors: series_id, series_path (dir), genesis_path (series.json), arming_cache, breaker_cache, checkpoint_cache, halt_sentinel, lock_path, commands_inbox/applied/rejected, heartbeat_path, ledger_dir, release_dir(h), process_base_dir(h, pid); creates ledger/ + commands/* eagerly; genesis keys {series_id, created_ms, schema_version}.
- Envelope = caller record merged flat with the 9 assigned fields; body carrying any of them refuses; payload_digest = canonical_hash(caller record incl kind+id). scan(since_seq) exclusive. head() fresh = (0, "0"*64). Torn tail = final line without "\n".
- batch fsync on n records OR ms elapsed (injected clock); barrier() resets; state.apply(full envelope), not called on dedup; snapshot_every needs state.
- Checkpoint(release_hash, last_tick_at, last_completed_tick_at, pending, positions_snapshot_at, schema_version, head_seq, head_hash); write via durable_write_json; load; validate_against(ledger) -> vocab.CACHE_STATES member or refuse.
- Fixtures `serve`, `open_ledger` local to test_ledger.py.
## Rulings (orchestrator) — plan edits to batch for skeptic review:
- [safety] vocab.MONEY_FIELDS closed tuple (qty, notional, limit, price, fee, avg_price, filled_qty, remaining_qty, amount, total, available, payout, avg_cost, reference_price, exposure, nav, bid, ask, mid); ledger refuses float only under these names (recursive); other floats legal (ratios). §5.8/§8 text to say so.
- [bookkeeping] vocab.CACHE_STATES = (current, stale); Ledger.close(); first_bad_seq = the seq the walk expected at the first failing position; snapshot_every = ledger.py named default DEFAULT_SNAPSHOT_EVERY, no grammar change.

## T2 document/release tests DONE (committed bd5eb7c). Pinned:
- document.py: ServeDocument.from_obj/load/to_obj/identity_obj/doc_hash; PRODUCTION_NON_IDENTITY_SECTIONS lives in document.py; doc_hash = config_hash(self, exclude=...) (name IS hashed); to_obj fresh deep copy, exact round trip, optional keys never materialised; fixed keys -> nested attribute access; user-named maps (guards, monitors, health.probes, alerting.sinks, alert_endpoints, heartbeat.emitters, serving.replay) -> Mappings; alerting.routes -> sequence with .severity/.sinks; params blocks opaque (except D9 check reading guards.*.params.measure/window); 20 required sections (placement required; alert_endpoints/heartbeat/env optional); refusals name the key; >=7 problems accumulate; NaN/Inf refuse at validation; ttl_ms strictly > 2*(renew_every+renew_timeout); bounds per §5; max_venue_skew_ms explicit null allowed (null != absent for identity). D9 live limits + fsync none-only-at-shadow validated at DOCUMENT level via a rung-keyed TABLE (no if rung ==). "arm rung equality" read as: live rungs need a child ApprovalVerifier class ref (deny-all refused at live).
- release.py: ReleaseManifest 15 fields in §5.3.1 order; release_hash property = canonical_hash(to_obj()); feed_spec = plain default-deny mapping of §5.2's 8 keys (no feed import); execution_scope = ExecutionScope; RuntimeFingerprint(python_implementation, python_version, cache_tag, abi, platform, libc, distributions, project_digests, image_digest), capture(project_files=()); Distribution(name, version, direct_url, record_digest); fingerprint_class = canonical_hash({ref: class_ref(cls), source: getsource}); artifact_digest(file|dir); ReleaseReader(manifest, allowed_names, root) get->bytes, names(); verify_release(manifest, root, now_ms, max_age_ms, source_config_hash=None) -> None or ProductionError ('artifact_expired' literal reason); parse_iso_duration; write_release(root, manifest, document) -> dir, idempotent, refuses differing.
- Shared fixtures in tests/production/test_document.py: example_document, minimal_document, live_capable_document(rung), set_path, drop_path, refusal. Invocation -> bundles.py (T10 pins).
- Plan gaps: §8 lists Invocation nowhere (bookkeeping); §10 release-before-feed resolved by mapping; verify_release source_config_hash kwarg; write_release takes a plain root path (ServeRoot delegates later).

## T1 vocab/base/redact/records/purity tests DONE. Pinned:
- records.py digests are METHODS: Intent.intent_digest(), DecisionPlan.decision_plan_digest(), ReductionIntent.reduction_intent_digest(), AccountState.risk_digest(); EvidenceRequirement.requirement_digest is an attribute set in __post_init__ (not a field). to_obj fully JSON-ready (Decimal->str); from_obj restores tuples/nested records (round trip equality). EntryBatch.watermarks_by_key: key -> InputWatermark. TickResult.legs = tuple of serialized leg dicts; TickResult.feed requires all 7 §6 members; latency_ms keys ⊆ TICK_PHASES; leg_latency_ms ⊆ LEG_LATENCY_BUCKETS. Every field named in MONEY_FIELDS carries Decimal.
- vocab: SEVERITY_LEVELS[sev] inner keys "otel","syslog","logging" (+ optional "pagerduty"); EXIT_CODES = {"stopped":0,"error":1,"halted":3,"already_running":4,"refused":5} (a map); METRIC_LABEL_VALUES[metric][label] = tuple, one entry per METRIC_NAMES; TICK_STATUSES colon-qualified (skipped:stale) — only non-snake vocab. MONEY_FIELDS + CACHE_STATES pinned.
- redact.py API: resolve_secrets(env_config), register_secret(value: str|bytes), redact(text) (non-str refuses), get_logger("<module>") -> cached dskit.production.<module> logger with exactly one redacting filter, REDACTED constant. ONLY names in env.require are registered as credentials (safety ruling by test: registering all env values shreds logs).
- test_purity imports VENUE_NAMES, _imports, _venue_hits from tests.pipeline.test_purity.
- Plan gaps: §8 lists CALENDAR_WINDOWS twice; EXIT_CODES is a map; requirement_digest normalisation tested as form only; readiness_digest left to test_readiness.

## T3 clock/sessions/cadence tests DONE (113 tests; reference sketch at scratchpad/ref/prod_*.py). Pinned:
- clock.py: ManualTime(now_ms=0) .now_ms/.set/.advance(refuses negative)/.monotonic (float s, never follows a backwards set); WallClock(); TestClock(start_ms=0) (set/advance; `__test__ = False`); ReplayClock(manual_time=None); both fakes expose shared instance as `.time`; sleep_until(epoch_ms, wake) -> True reached / False woke; TestClock jumps manual time, ReplayClock neither blocks nor advances; WallClock reads time.time/time.monotonic/time.sleep as module attrs, slices <= 1.0 s.
- sessions.py: cls(params); next_open/next_close strictly after after_ms or None; [open, close) half-open; buffers shift effective bounds; buffers consuming a session refuse; fall-back fold=0 ok; gap refuses naming HH:MM; close<=open refuses; Composite {"members": [{uses, params}]}; Calendar.window(kind, at_ms) -> (start, end) concrete on ABC over vocab.CALENDAR_WINDOWS (session/day/event; AlwaysOpen windows UTC day); Calendar.tz_name (UTC default; WeeklySessions zone; Composite first member).
- cadence.py: next_tick(after_ms, calendar) -> first grid instant strictly after, open per calendar, or None; FixedInterval anchor_ms default 0; AlignedBar k*bar_ms+publish_delay_ms (delay default 0); AtTimes requires times+relative; relative clock -> HH:MM in calendar.tz_name filtered by is_open; open/close -> int second offsets, anchored on session under way, NOT filtered by is_open; anchor strategy via mapping; OnData after+poll_ms (>=100) else next open.
- Overrun(params) _PARAMS (policy, max_lag_ms); DEFAULT_OVERRUN_POLICY="coalesce", DEFAULT_MAX_LAG_MS; resolve(due_ticks asc, now_ms) -> (tick_at|None, absorbed); coalesce: latest, (None, all) when now-latest > max_lag (inclusive ok); skip: freshest always; queue: oldest, nothing absorbed. Strategy objects keyed by vocab.
- Gaps: window helper name (pinned); tz_name (pinned); GAP_SCAN_YEARS named constant for DST-gap scan (no datetime.now in validate_params).

## T5 state tests DONE (67 cases, mutation-checked). Pinned:
- SeriesState(series_id, max_history=DEFAULT_MAX_HISTORY); apply(envelope) refuses non-dict/unknown kind/non-dense seq/foreign series_id; snapshot()->StateView; head(); restore(snapshot_env) (drops executor/accounting tokens, keeps economic_seq; refuses at_seq mismatch / started fold); to_snapshot_obj() (payload only); open_ticks()->tuple[TickStart]; monitor_state()->MappingProxyType.
- StateView 14 members in §5.8.1 order; tuples positions/pending/decision_history; MappingProxyType working/balances/guard_holds/pending_control; working[ref] = OrderState joined from intent proposal; balances[ccy] Decimal; guard_holds[(guard, scope_key)]; pending_control[request_id]=purpose; reduction.{authority_id, rights, reserved, expires_ms}. TickState 5 members, no rung.
- Fold: intent->pending (not economic); order_event->working by status in TERMINAL_STATUSES, economic; fill->book (+filled_qty), reversed undone once; cash_flow->balances; trip->breaker (leaving active clears arming); authority issue/disarm/revoke; authority_use reserves (not economic).
- Recovery(ledger, state, id_source, executor).run(clock) -> RecoveryReport{replayed, closed_ticks, queried_refs}; appends tick(failed)+decision(legs=[]), order_event per unmatched ref (status from answer; unknown on raise), process{event: recovered} last; never submits.
- AST: .scan( only in state/ledger/reconcile/__main__/outcomes/report; self.<fold attrs> = only in state.py/ledger.py.
## RULINGS after T5 (safety-critical; plan §6/§5.8 edits to batch):
- R1 NESTED ENVELOPE: a ledger record is {kind, id, payload_digest, seq, series_id, process_id, release_hash, recorded_at_ms, schema_version, prev_hash, hash, body}; the caller passes {"kind", "id", "body"} and nothing else (any other top-level key refuses); body is a dict and may carry its own kind/release_hash/series_id (a recovering process's envelope release may differ from a tick's); payload_digest = canonical_hash({kind, id, body}); hash = record_hash(prev_hash, envelope-minus-hash); MONEY_FIELDS walk over body. Field renames from T5 stand (authority.role, guard_state.state_kind, outcome.outcome_kind, cash_flow.flow_kind, control_approval.verified_payload_digest) to keep bodies self-describing.
- R2 snapshot: JsonlLedger.snapshot(payload) takes the JSON-able payload; auto-snapshot calls state.to_snapshot_obj(); body = {at_seq, state_digest, state}.
- R3 PositionBook keeps per-instrument applied-fill log (fill_id, side, qty, price, fee) since the position was last flat; reverse(fill_id) recomputes exactly from the log minus that fill; unknown/realised fill_id refuses; the log is part of the snapshot payload (restart-safe reversal).
- R4 economic_seq: outcome and adoption do NOT advance it; cash_flow does.
- R5 vocab.AUTHORITY_EVENTS = (issue, disarm, revoke, expire); authority issue body for role ordinary embeds arming = ArmingState.to_obj() (maker/checker/proof digests producers); authorization body = {permit: ActPermit.to_obj(), authority_use_id}.

## T6 resilience/metrics tests DONE (185 tests). Pinned:
- Retry(params,*,clock,sleeper,rng): decide(attempt 1-based, kind, is_write) (refuses ok/unknown/attempt<1); backoff_s(attempt, kind, retry_after=None) = base*2**(attempt-1), cap before jitter, Retry-After honoured un-jittered+capped; wait(...)->seconds via callable sleeper(s); refund(); budget.{capacity,balance}; ambiguous READ retry charged transient_cost. Constants DEFAULT_{MAX_ATTEMPTS,BASE_S,THROTTLE_BASE_S,CAP_S,JITTER,RETRY_AFTER,RETRY_WRITES,BUDGET_CAPACITY,TRANSIENT_COST,THROTTLE_COST,REFUND,MIN_CALLS,FAILURE_RATE,OPEN_S,MAX_IN_FLIGHT,CONNECT_S,READ_S}, MAX_ATTEMPTS_BOUNDS=(1,10), MAX_BACKOFF_S is onboarding.connector's.
- CallOutcome(status, exception, request_sent) frozen; HttpClassifier().classify; no CLASSIFIER_KINDS.
- CircuitBreaker(params,*,clock): record/allow/state/trip/reset/observe_only (-> metrics_only); CircuitBreakers(params,*,clock).for_scope memoized.
- RateLimiter(params,*,clock): both lanes required; acquire/release/in_flight/next_allowed_ms/observe(lane, headers).
- Transport ABC; UrllibTransport(params).send -> (int, dict, bytes), non-2xx returned, urllib timeout = max(connect_s, read_s), urlopen by attribute with Request; TRANSPORT_KINDS == ("urllib",).
- resilience_from_document(section,*,clock,sleeper,rng) -> ResiliencePolicies(retry, breaker(CircuitBreakers), limiter, transport) frozen.
- Metrics(log_dir=None, labels_max_cardinality=DEFAULT_LABELS_MAX_CARDINALITY): counter/gauge/histogram(name, labels=(), buckets=None); label values on the record call (inc(n=1, **labels) / set(v, **labels) / observe(v, **labels)); series key "k=v,k=v" sorted, "" unlabelled; _total <=> counter; flush(at_ms, tick_id)->bool + Metrics.flush_failures; snapshot(); histogram series {count,sum,buckets{...,"+Inf"}}; RESERVED_LABEL_VALUE="other", METRICS_FILENAME, INF_BUCKET, DEFAULT_BUCKETS.

## T10 ids/bundles/policy tests DONE (332 cases; golden table 17,280 cells at tests/production/policy_golden.json, 1.65 MB — owner may prefer submit-only). Pinned:
- ids: IdSource ABC abstract next_tick_id/leg_id/plan_id/client_ref; concrete flatten_client_ref == canonical_hash(("flatten-v1", release_hash, request_id, index, reduction_intent_digest)); ids 64-hex; ReleaseIdSource(release_hash); AST ban on time/random/uuid/secrets/datetime/os in ids.py; RecordedIdSource(tape of (method, args, id)) refuses divergence/exhaustion/malformed.
- bundles: seven frozen dataclasses presence-only; imports only base/vocab; Invocation(armed, env_release_hash, once, max_ticks>=1) lives in bundles.py.
- policy: PolicyDecision(allowed, reason); Rule(name, veto) frozen + AllowRule(Rule), decide(subject) -> PolicyDecision|None; ActionPolicy(rules=ACTION_RULES) 13 rules in order (priority_lane, submit_requires_ready_health, submit_refused_while_halted, submit_blocked_by_pending_control, reduction_origin_requires_reducing, model_origin_refused_while_reducing, reducing_permits_only_proven_reductions, live_submit_requires_go, live_submit_requires_ordinary_arm, live_reduction_requires_reduction_right, simulated_submit, live_ordinary_submit, live_reduction_submit); TransitionPolicy(rules=TRANSITION_RULES) 6 rules (transition_not_permitted, transition_requires_proof, verified_reduce_enters_reducing, verified_flatten_enters_reducing_from_halted, trip_enters_halted, verified_resume_enters_active); decision_table(policy) keyed "rung|breaker|health|readiness|operation|risk_effect|origin|authority|pending_control" (authority None->"none"); fall-through refuses with a non-rule reason; rung axis = module-level lane table keyed by request.rung (no rung ==).
## RULINGS after T10 (accepted as pinned): model origin while reducing allowed below live per D10 (shadow any effect, paper reduce only), refused at live; GO required at live rungs in reducing as well as active; authority axis inert below live; pending_control blocks submit only. Bookkeeping: vocab.TRANSITION_CAUSES = (reduce, flatten_request, trip, halt, resume); §5.14 "eight-field" -> nine; Invocation -> bundles.py in §8; §5.14 to state the lane-table rule.

## F1 foundations BUILT (510 passed, 75 skipped; ruff clean; pipeline purity 15 passed). Decisions:
- base.GENESIS_HASH exported; _raise_if re-exported by identity raises AssetError — production code raises ProductionError(problems) directly; utc_iso emits +00:00 ms precision.
- records: private _Record base coerces from field annotations (Decimal|None, tuple[X,...], dict[str,X], nested records); _CLOSED table; _check(problems) hook; opaque dicts (native, extra, outputs, labels, legs[i], feed, error) JSON-shaped; float under MONEY_FIELDS refuses at any depth; OrderState(Ack); Ack.venue_ref/avg_price optional; TickResult.data_asof_ms/coverage_digest/inputs_digest optional; feed exactly 7 members; ReductionPlan refuses mismatching digests; risk_digest excludes native, *_ms stamps, asof_ms, versions. Finding.value/bound and MeasureEvidence.value are Decimal-only (ratios -> Decimal(str(x))).
- redact: only get_logger attaches the filter; whole URL masked; empty/"[REDACTED]" secrets refused; an EMPTY required env value refuses.
- vocab beyond §8: AUTHORITY_EVENTS, TRANSITION_CAUSES, ALERT_SUPPRESSIONS; TRIP_REASONS spelled <source>_<condition> + monitor_alarm; RECON_ACTIONS=(none, refuse, halt); LIMIT_SCOPES=(aggregate, per_key, group); refusals_total{reason} = skipped:* + refused + VERDICTS; submits_total{outcome} = STATUSES.
- RULING: alert_sink_failures_total{sink} values = core sink kinds; monitor_verdicts_total{monitor} values = the nine monitor kind names (pin equal to registries later).

## T15 monitors tests DONE (104 tests, numbers verified). Pinned:
- cls(params, name=None); MONITOR_KINDS.resolve(uses)(params, name=key); common _PARAMS window/threshold/response/min_n (DEFAULT_MIN_N); +field(+reference) for stream/distribution; psi adds bins>=2; ks refuses bins; page_hinkley adds delta (lambda from threshold); tracking_signal adds target_field; latency adds percentile; notes allowed at every params/selector site.
- reference: one {uses, params} site or a list; both kept; verdict reports the worse; each reference bins on its own quantiles; fed by fit(records) and by each chunk as it closes; Reference.add(value) concrete, sample() abstract; Snapshot no I/O at construction, refuses missing file at fit.
- Threshold: breached(statistic, n_ref, n_cur) abstract + concrete bound(n_ref, n_cur); breach -> alarm iff response == halt else warn; should_trip() == (status == alarm); Alpha(params, benchmark=...) injected by DistributionMonitor.critical_value(alpha, n_ref, n_cur), refuses without benchmark; reference_std(k) = k*pstdev over prior statistics (<2 priors never breaches).
- Chunkers: Count yields trailing partial chunk; Sliding full windows only; Period({"iso": ..., "time_field": "data_asof_ms"}) imports parse_iso_duration from release.py.
- verdict(): current window; partial or n < min_n -> insufficient; slice "all"; window "count:4" style; provisional False for all nine kinds. Observation rule: one per legs[] entry (zero-leg decision counts once for Coverage) else the record itself.
- Reductions: Staleness max; LatencyPercentiles p95 (percentile knob) over summed latency_ms; DecisionRate = count of decided ticks per window; Coverage = abstaining fraction (§4.1 example should be max: 0.5 — plan bookkeeping).
- Profile.from_records(records, fields, bins, top_k); merge exact/associative; to_obj/from_obj default-deny.

## T11 pipeline-seam tests DONE (152 tests in tests/pipeline/: test_subgraph_runner 49, test_execution_policy 15, test_serving_effect 67, test_serving_contract 21; guards green; 20 hashes). Pinned:
- SubgraphRunner(the_plan, needed, node_outputs, splits_info, prev, policy) positional; rerun(overrides, outputs, ctx, prev_bindings, *, guard_verdicts=False) -> (outputs, ran tuple in plan order, seconds dict keyed by ran); run_keys(keys, outputs, ctx, prev_bindings) same shape; node_outputs is read-only context (scratch outputs never touch it); deferred key with no seeded output -> ValueError naming the key (never KeyError).
- apply_param_override public with `_apply_param_override = apply_param_override` ALIAS kept (test_kinds_search imports the private name); exactly one FunctionDef.
- ServingContract refusals ConfigError; ObservationRows.serving_contract refusals ValueError-compatible; ExecutionPolicy.classify params compared by ==; classify_plan returns dict ordered by plan.order; ConfigError.errors carries the list; NodeContext.release_reader is the 11th and last field.
- Real method is FittedTransform._sidecar (not _read_sidecar); observations/FeatureSelector are NOT registered by `import dskit.pipeline` (19 kinds) — tested by class; KIND_EFFECTS/CLASS_EFFECTS/LOAD_EVIDENCE exported by test_serving_effect.py; fixtures fixture_pipeline/planned/base_pass/a_runner/a_policy/graph in test_subgraph_runner.py.
- Pre-existing env failures under uid 0: test_mlflow TestBusyLocalStore unwritable parent; test_runs unlistable nodes dir.

## T9 breaker/arming/coordination tests DONE (226 tests). Pinned (see agent report for full list):
- Breaker(document, serve_root, *, ledger, state, clock, transition_policy, executor=None); current(view); trip(reason, actor, control_request_id=None, principal_digest=None, proof_digest=None, cause="trip") -> seq; reduce/flatten(actor, control_request_id, principal_digest, proof_digest); reset(actor, acknowledges_trip_id = the latest trip record, ...); halt_sentinel_present(); create_halt_sentinel()->bool never raises; retire_halt_sentinel(purpose in resume|flatten_request)->bool; cache trio; cancel precedence none/failed/unknown/submitted/partial; executor.cancel_all() -> tuple[Ack].
- Arming(document, release, *, serve_root, verifier, clock); request(arm_request, request_id); approve(arm_approval, request, request_id, approval_id) -> (authority_body, ArmingState); current(view, at_ms); check_conjunction -> ConjunctionResult{satisfied, reason} with closed reasons; apply_scope(proposal, arming_state); effective_bounds; revoke/disarm/expire_if_due(view,...); allowlist ⊆ release.feed_spec.required_keys; overlay may only lower a declared max / raise a declared min on a named guard; principal_digest = canonical_hash(principal.id); expiry inclusive (at_ms >= deadline expired). ApprovalVerifier abstract verify + concrete check_purpose; LIVE_CAPABLE=False on ABCs; verifier_fingerprint(cls, params); approval_verifier(document). ReductionRights(*, clock).from_plan(plan, approval, authority_id, expires_ms) (refuses duplicate (instrument, side, qty, limit)); reserve(view, digest, client_ref); revoke_unused(view, reason).
- coordination: Lease 4 abstract + concrete permit_current; LeasePermit(scope, holder, fencing_token, expires_ms); ProcessLease({}, clock=...); LeaseRenewer(lease, permit, *, clock, every_ms, timeout_ms).tick(now_ms), .permit/.invalidated; scope_equal(*scopes).
## RULING R6 (safety-critical, §6 edit): the halt `trip` record is appended+barriered BEFORE cancel I/O and carries NO cancel_outcome; a new record kind `cancel_outcome` {trip_id, outcome ∈ CANCEL_OUTCOMES, acks[]} is appended after the cancel attempt (non-economic; fold no-op beyond head). Recovery: a halting trip with no later cancel_outcome → re-issue cancel_all (query-first) — phase-1 pins the record; the recovery re-issue is loop/recovery work (note for G18/G5).
## RULING R7: ledger.py exports one `validate_cache_head(head_seq, head_hash, ledger) -> CACHE_STATES member | ProductionError`; Checkpoint/Breaker/Arming import it.

## F4 ledger BUILT (154 passed). Decisions:
- JsonlLedger(serve_root, process_id, release_hash, *, clock, fsync="every", rotate=None, state=None, snapshot_every=None); batch ms from first pending append (clock.monotonic()); barrier/roll/close reset; roll seals old segment + fsyncs dir; reads work after close, writes refuse; state.apply gets json.loads(line); snapshot id "snapshot-{at_seq}"; rotate {by, max_bytes, notes}, default by=day; process rolls off inherited segment at first append; open refuses a non-JSON complete line; hash/order damage located by verify(); ServeRoot.segment_path(i)/segment_paths(); SCHEMA_VERSION = 1 in ledger.py; validate_cache_head exported; Checkpoint.load -> None when absent.
- Gaps: max_bytes optional in every rotate mode, required for size (plan text); fsync batch grammar {"batch": {"n", "ms"}} — test_document pins strings only (document/compose must agree); money walk duplicated (records._json_value vs ledger._check_money, differing list semantics) -> graduate one owner to base.py (REVIEW FIX); _fsync_dir imported privately from onboarding.base -> export publicly (REVIEW FIX).

## T8 guards tests DONE (352 tests, 28/28 mutations caught). Pinned:
- Guard(params, name=None); Limit(params, name=None, measure_registry=MEASURE_KINDS); RangeGuard(params, name=None); GuardChain(guards: ordered name->Guard; key != guard.name refuses); Measure() no params; Measure class attrs kind/scalable/scalable_field/window_kinds; Guard.requirements(candidate, at_ms, calendar) concrete returning () (RangeGuard inherits); Guard.hard = breach verdict != amend; second pass only when something was amended.
- Measure.value(proposal, state, window, scope_key, include_working) rebuilds the requirement from state.account.asof_ms + state.calendar; Limit.amend(proposal, state, value) postcondition-tested, rounded toward zero; GuardChain.check_authority_scope(proposal, state, scope); overlay looser than document raises; unknown guard raises; public surface exactly requirements/check_all/check_authority_scope/guards.
- hold expiry: state.account.asof_ms is "now"; held_until_ms > asof_ms refuses (reason starts "held"); pause sets held_until_ms == resume_at_ms; Limit.guard_state_body(finding, at_ms, calendar); state_kind = on_breach.
- Module names: max_verdict, BREACH_VERDICTS (pause->hold), Window(kind, arg) + from_params + label + resolve(at_ms, calendar) -> (start, end, baseline, arg), Bound(min, max), Scope(kind, field); scalable_field: quantity/exposure_after -> qty, notional/bankroll_fraction -> notional; Finding always Decimal, None on held/missing/stale/NaN; amend with min-only bound refuses; notes accepted; parse_iso_duration imported from release.py.
- requirements: aggregate -> "*"; per_key/group -> per candidate.scope_keys; dedup by digest, insertion order.
## RULING R8 (safety, for G7 accounting + G17 leg): Accounting.snapshot(state_view, executor, quotes, at_ms, requirements, calendar) RE-ANCHORS every requirement at its own at_ms (bounds from window_kind + window_arg via Window.resolve(at_ms, calendar)), keys measure_evidence by the re-anchored digest, and returns AccountState.asof_ms == at_ms; guards rebuild the digest from state.account.asof_ms so step (2)'s later snapshot still finds evidence. evidence_asof_ms bound into plan/intent/permit = account.asof_ms.
- Open: exposure of a market working order (limit None) — implementer documents the rule; amend infeasible -> Limit.check returns refuse (pin in review).

## PAUSED by the owner (all agents stopped). State at pause:
- GREEN and committed: G1 foundations (vocab/base/redact/records/purity, 510 tests), G4 ledger (154 tests). Red tests committed for: document/release, clock/sessions/cadence, state, resilience/metrics, ids/bundles/policy (+golden), monitors, breaker/arming/coordination, guards, pipeline seam (tests/pipeline/test_{subgraph_runner,execution_policy,serving_effect,serving_contract}.py).
- STOPPED MID-BUILD (files may be partial): F5 state.py (was writing dskit/production/state.py in full); F11 pipeline seam (had read the design; unknown how far into driver.py/node.py edits); R1 review of G1+G4 (had only started reading).
- TO RESUME: re-launch F5 (state), F11 (pipeline seam — check `git diff dskit/pipeline` for partial edits first), R1 review; then F2 document/release, F3 clock/sessions/cadence, F6 resilience/metrics, F10 ids/bundles/policy, F8 guards (after state), F15 monitors, F9 breaker/arming/coordination (after state+ledger); remaining test authors: G7 control/accounting (note ruling R8), G12 feed/decider+conftest (after F11), G13, G14, G16, G17, G18, G19, G20; S0 phase-2 spec; then reviews, docs, wrap.

## RESUMED (session 2). origin/main merged into the branch (8a1092b): main gained ADR-0092/0093/0094 work incl. driver.py changes (+140), folds.py, attempts.py, runs.py. The repo copy of these notes is now canonical.

## F11 pipeline seam BUILT (152 seam tests; driver/search 155 untouched; 20 hashes; gates green). Decisions for the decider group:
- policy.reader(k) is asked per executed node; non-None reader reaches the node as replace(ctx, release_reader=reader); Node.read_artifact_text returns ctx.release_reader.get(filename) and opens nothing.
- Base pass: SubgraphRunner(the_plan, needed, node_outputs, splits_info, prev, policy).run_keys(keys, outputs, ctx, prev_bindings) -> (outputs, ran, seconds); keys in plan order; needed does not filter; undeclared key -> ValueError; deferred key must be pre-seeded in outputs (else ValueError). $node refs absent from outputs resolve against node_outputs (ChainMap).
- TrainableNode.serving_effect reads evidence {"mode": "load", "artifact_pinned": True}. ObservationRows.serving_contract reads params only; SOURCE_BINDING_KIND/DIGEST_RECIPE_KIND exported by libs/observations.py; serve documents must declare "since_ms": null.
- Test-side fixes applied by orchestrator: grandfather list entry for _apply_param_override removed (test_method_lengths); audit test snapshots the registry in a fresh interpreter (synth_adapter registers synth-source in-process).
- Docs debt for G21: pipeline CLAUDE.md/README.md trees lack policy.py.
- Pre-existing from main: children/intraday_equities/tests/test_configs.py::test_every_run_reads_the_split_adjusted_store_from_the_study_start fails (ADR-0094 configs) — not ours; verify on main at wrap.

## F3 clock/sessions/cadence BUILT (113 passed). Decisions:
- Clocks built by keyword, not cls(params): WallClock(), TestClock(start_ms=0), ReplayClock(manual_time=None); compose: CLOCK_KINDS.resolve(uses)(**params); share time via ReplayClock(manual_time=test_clock.time); MAX_SLEEP_SLICE_S = 1.0.
- Calendar.window(kind, at_ms) -> (start_ms, end_ms) half-open, containing at_ms else the next; dispatch table from CALENDAR_WINDOWS to _window_<kind> hooks; None => ProductionError. WeeklySessions.session = effective session (buffers applied, blackouts ignored); EventWindow.session == event; Composite intersects; Composite.next_open fixpoint bounded by MAX_COMPOSITE_ROUNDS=64; DST validation scans GAP_SCAN_YEARS=(2024, 2030) + runtime refusal.
- Cadence.next_tick(after_ms, calendar): first grid instant strictly after and open; None when never reopens; ProductionError after MAX_SHUT_SPANS=64; at-times open/close anchor on calendar.window("session"); clock reads HH:MM in calendar.tz_name.
- Overrun(schedule.overrun) — the block IS the params; resolve(due_ticks, now_ms) -> (tick_at|None, absorbed_asc); coalesce -> freshest or (None, all) when now - freshest > max_lag_ms; skip -> freshest; queue -> oldest. DEFAULT_MAX_LAG_MS = 30_000. check_int_param from pipeline.
- Debt: the cls(params) idiom (__init__ -> validate_params -> _configure, notes allowance) written twice (Calendar; cadence._Configured) — graduate one Configured base into base.py at review (G7/G15 need it).

## R1 review of G1+G4 DONE — no blockers; fixes applied: base.reject_money_floats is the one money-walk owner (list inherits the money name; imported by records.py and ledger.py); public fsync_dir in dskit/onboarding/base.py (alias kept); +62 tests closing unproven refusals (divergent-at-head cache, torn line in sealed segment, non-JSON/non-envelope lines, money inside opaque payloads, ReductionPlan digests, append_many atomicity, reads-after-close, longest-first masking, two-way completeness pins). 726 passed.
## RULINGS after R1: R9 record `id` is unique across the SERIES (index keyed by id alone; digest covers kind) — producers derive record ids kind-qualified, e.g. f"{kind}:{semantic_id}" (tick_start and tick for one tick_id get distinct ids); pin in leg/loop tests (§6 edit). R10 Proposal: `qty` is the order size and is required when side != none; `notional` is informational/derived (§5.4 wording).

## F5 state BUILT (70 passed; cross-process recovery check). Decisions:
- StateView types: positions tuple[Position] (sorted, derived, native=None); working MappingProxy[ref -> OrderState] (avg_price/fee exact from fills); pending tuple[str]; balances MappingProxy[ccy -> Decimal]; decision_history tuple[MappingProxy] (leg dict + tick_id, newest max_history); breaker str; arming ArmingProjection|None (11 ArmingState fields; to_obj exact -> ArmingState.from_obj(view.arming.to_obj())); readiness ReadinessProjection|None (5 ReadinessResult fields); guard_holds MappingProxy[(guard, scope_key) -> MappingProxy(body)]; reduction ReductionProjection|None {authority_id, rights, reserved, expires_ms} + .reserve(digest); pending_control MappingProxy[request_id -> purpose]; risk_version RiskVersion(economic_seq, None, None).
- Fold: apply requires dense seq, own series_id, prev_hash == head; ANY trip clears arming; disarm/revoke/expire clear only the matching authority_id; authority_use refuses non-current/ungranted/re-reserved; unknown-ref order_event tolerated; fill via Fill.from_obj (extras ignored); duplicate fill_id in live log refuses; order_event carries no qty -> working OrderState joined from the intent's proposal (qty:null proposal refuses at intent).
- Accessors: series_id, monitor_state(), open_ticks() (TickStart; release_hash falls back to envelope), undecided_ticks(), tick_plans(tick_id). Snapshot payload = view members + monitor_state, open_ticks, tick_plans; positions = PositionBook.to_obj() (fill logs); restore key-exact, drops tokens, does not verify state_digest.
- Recovery ids: recovered-{tick|decision}-{tick_id}, recovered-order_event-{ref}, recovered-process-{head_seq}; derived ref = id_source.client_ref(tick_id, leg_index, 0); an intent matches a plan by id AND decision_plan_digest; one barrier() at end.
- Gaps: monitor window state cannot be derived from monitor records — Monitor.state() into the snapshot needs a seam (loop: state.set_monitor_state? -> G15/G18 to pin); _money restates records._decimal (review to graduate); R6 halt-cancel re-issue on recovery is loop/breaker work (G18).

## F2 document/release BUILT (307 passed after the one test fix: a sink url_env must be in env.require — §5.11 — so the excluded-section identity case now moves env.require and alert_endpoints together). Decisions:
- Reading a document: fixed keys by attribute; author-named maps/params are MappingProxyType (dict(site.params) for plain; or doc.to_obj()[section]); lists are tuples; absent optional key -> None (owner applies its DEFAULT_*); doc.coordination.scope IS an ExecutionScope; doc.durability.fsync is str or {"batch": ...} proxy — hand doc.to_obj()["durability"]["fsync"] to JsonlLedger.
- Exported one-owner keys/bounds: OVERRUN_KEYS, RETRY_KEYS, RETRY_BUDGET_KEYS, BREAKER_KEYS, LIMITER_LANE_KEYS, RATE_LIMIT_KEYS, ROTATE_KEYS, FSYNC_BATCH_KEYS; SHUTDOWN_GRACE_S_BOUNDS, GROUP_WAIT_S_BOUNDS, REPEAT_INTERVAL_S_BOUNDS, MIN_HEARTBEAT_EVERY_S, MIN_VALID_FOR_S, RENEWAL_BUDGET_FACTOR; D9 names SIZE_MEASURES, LOSS_MEASURE, PAPER_ACCOUNTING_KIND, DENY_ALL_APPROVAL_KIND, FSYNC_NONE; check_fsync public. series_id canonical lowercase-hyphenated.
- plan builds ReleaseManifest(series_id, doc_hash, ..., artifacts={name: {digest: artifact_digest(p), timestamp_ms}}, classes={key: {ref: class_ref(cls), code_digest: fingerprint_class(cls)}}, feed_spec=<FEED_SPEC_KEYS>, execution_scope=doc.coordination.scope, runtime_fingerprint=RuntimeFingerprint.capture(project_files, image_digest), created_ms=now_ms()) then write_release(series_root, manifest, doc); max_age_ms = parse_iso_duration(doc.serving.max_artifact_age).
- verify_release problem strings start with ARTIFACT_MISSING | ARTIFACT_DRIFT | ARTIFACT_FUTURE_DATED | ARTIFACT_EXPIRED | RUNTIME_DRIFT | SOURCE_CONFIG_DRIFT; runtime recapture ≈38 ms (loop may cache within a tick); the distribution inventory follows sys.path (plan and serve must launch the same way). ReleaseReader.get returns BYTES (node.read_artifact_text now decodes); refuses directory artifacts.
- Review debt: "releases" spelled in ledger.ServeRoot.release_dir and release.RELEASES_DIRNAME (one owner); ledger._durability vs document.check_fsync (ledger should call it); rotate keys -> ROTATE_KEYS; _strip_notes third private copy (make pipeline.base._strip_notes public, §9.1). Silent readings: waivers = list of item names; probes accept timeout_s; image_digest via capture(image_digest=); rotate.by=size without max_bytes passes validate (ledger refuses at open).

## F10 ids/bundles/policy BUILT (334 passed after records.PolicyRequest.authority became str|None). Decisions:
- Leg -> policy: action_policy.permits(PolicyRequest(operation="submit", risk_effect=evaluation.risk_effect, rung=bindings.rung, origin=bindings.origin, breaker=<fresh view>, health=<health state>, readiness="go"/"no_go" (expired => no_go), authority="ordinary"|"reduction"|None from the fresh view, pending_control=bool(view.pending_control))) -> PolicyDecision(allowed, reason); priority-lane requests pass risk_effect="neutral"; fall-through reason "unclassified"; permits refuses a non-PolicyRequest.
- Breaker -> transitions: transition_policy.permits(from_state, to_state, cause, proof_digest); None/empty proof = absent; trip/halt need none.
- compose: bundles positional in §5.16 order or keyword; presence-only; ActionPolicy()/TransitionPolicy() no args; Invocation(armed, env_release_hash, once, max_ticks>=1|None); ReleaseIdSource(release.release_hash); RecordedIdSource(tape of (method, args, id)).
- Loop ids: tick_id = next_tick_id(tick_at_ms) before tick_start (record id f"tick_start:{tick_id}" per R9); per leg before step (1): leg_id(tick_id, i), plan_id(tick_id, i), client_ref(tick_id, i, attempt=0); reduction legs: flatten_client_ref(plan.release_hash, plan.request_id, intent.index, intent.reduction_intent_digest()) (concrete on the ABC, no tape entry). Recipes tick-v1|leg-v1|plan-v1|client-v1.
- Review debt: document._pinned restated inline in policy.py (graduate to base.py); a public digest-shape check for ids/ledger.
- Stray untracked tests/production/test_zz_smoke.py seen (in-flight agent file) — remove if orphaned.

## F6 resilience/metrics BUILT (396 passed). Decisions:
- compose: resilience_from_document(document.to_obj()["resilience"], clock=, sleeper=, rng=) -> ResiliencePolicies(retry, breaker=CircuitBreakers, limiter, transport); problems prefixed "resilience.<block>:"; sleeper = lambda s: clock.sleep_until(clock.now_ms() + int(s*1000)).
- executor call shape: breakers.for_scope(s).allow() -> limiter.acquire(lane) (False = don't send; next_allowed_ms(lane)) -> transport.send(..., transport.timeout) -> always limiter.release(lane), limiter.observe(lane, headers), breaker.record(outcome); outcome = HttpClassifier().classify(CallOutcome(status, exc, request_sent)) (executor sets request_sent); retry.decide(attempt 1-based, outcome, is_write); retry -> retry.wait(attempt, outcome, retry_after=); success -> retry.refund(); reconcile -> query, never resend. Limiter = exact GCRA bucket in integer ms; module reads only clock.now_ms().
- loop/alerts: Metrics(log_dir=placement.log_dir); declare with labels=tuple(sorted(METRIC_LABEL_VALUES[name])); record with label kwargs; flush(at_ms, tick_id) once per tick outside barriers, never raises; flush_failures for health.
- Readings: `reserved` lane = a Retry-After observed on a lane holds that lane AND every non-reserved lane; a reserved lane is held only by its own observations. Half-open + fatal probe -> closes. Retry takes clock but has no deadline knob (unused) — review: add deadline_s or drop. Lane/section keys restated privately (export from document.py at review). Third copy of the cls(params) idiom — graduate to base.py.

## T12 feed/decider tests + conftest DONE (test_feed 68+, test_decider 103; conftest 14 fixtures over a REAL synthetic run: localfiles source `local`, stream `bars`, 7-node training doc bars(observations, since_ms:null)->weights(SideTable, pure, the only base-pass node)->usable(filter)->grid(event-grid)->scaled(standardize)->scored(derive)->picks(join, HEAD); fixtures synthetic_root/synthetic_registry/source_config_hash/training_document/training_run/run_dir/entry_params/serving_contract/feed_spec_obj/release_manifest/serve_document/fresh_root/clock/recorder; module constants SOURCE/STREAM/UNIVERSE/ENTRY_NODE/ENTRY_PARAM/WINDOW_MS/HEAD/BASE_PASS_NODE/TRAINABLE_NODE/SIDECAR/...; imports minimal_document from test_document). Pinned:
- feed.py: Feed ABC abstract pull(tick_at_ms)->FeedResult; EntrySourceFeed(params, *, root, registry, contract, spec, clock, max_staleness_ms, dead_after_ms); DEFAULT_PULL_MODE; FeedSpec frozen 8 fields in release.FEED_SPEC_KEYS order + from_contract(contract, required_keys, source_config_hash, source_config_version) + to_obj/from_obj; snapshot_entry(contract, spec, entry_outputs, source_config_hash) -> EntryBatch (module function; ServingContract has no methods); active_source_identity(registry, source) -> (hash, version) the ONE owner for plan and every pull; ReplayFeed(params, *, tape of (FeedResult, EntryBatch)) with pull()+batch(); FEED_KINDS == ("entry-source", "replay"). Freshness ladder measured against tick_at_ms; FeedResult.at_ms from clock; inclusive thresholds (age <= max_staleness -> live, <= dead_after -> stale); run_acquisition/scan_stream read as module attributes; scan_stream runs in BOTH pull modes (watermarks at fetch); a required key absent from the store -> dead; source drift raises ProductionError BEFORE any fetch, failure during pull -> dead; rows port = the single list-valued output port.
- decider.py: serving_document(run_document, run_dir, heads, replay) resolving against DEFAULT_NODE_KINDS + class refs (adapter imported first); recorded outputs come from carry.json (nodes/NN-*.json are always summarised) — node absent from carry.json refuses; every search node must carry a recorded winner (fail-closed); Decider(document, release, *, registry, adapter, proposer, clock) with prepare(asof, base_run_dir)->dict (the classify_plan answer) + serving_hash attr, read_entry(tick_at_ms)->EntryBatch, evaluate(batch)->(head_outputs={head: outputs}, head_digest=canonical_hash(head_outputs)); ServingExecutionPolicy(entry, *, release, root); RecordedOutputs (role gate, class outputs None, instance outputs = recorded names, params {"outputs": {...}}, serving_effect pure) + RecordedStatTest(RecordedOutputs) role stat_test; Proposer ABC abstract candidates/proposals, concrete quotes; IntentRows/TargetPositions; PROPOSER_KINDS == ("intent-rows", "target-positions"); candidate id derived from the WHOLE head row; policy.reader(key).names() == (SIDECAR,) and get(SIDECAR) -> bytes (node-scoped reader).
## RULINGS after T12: R11 release FeedSpec.digest_recipe is a dict (release.py _check_dict; test fixture updated). R12 ReleaseReader gains a per-node scope: ReleaseReader(manifest, allowed_names, root, prefix="") — names() relative to the prefix, get(name) resolves prefix+name; the policy's reader(key) is scoped to f"artifacts/{key}/". R13 stat_test role is toolkit-OWNED: the decider registers recorded-gate/recorded-stat-test kinds owned=True into the serving registry copy it plans with (production is dskit, not a child). R14 source_config_version = str(count of registered versions of the alias) (monotone label); source_config_hash = the active alias's version_id. Accepted as pinned: drift-raises-before-fetch; scan_stream in both modes; carry.json as the recorded-outputs source; every search node needs a winner; batch validation is the loop's. Plan text edits: §5.2 "identity failure" sentence; §5.3 recorded outputs from carry.json; §5.3 Decider constructor; §5.2 FeedSpec.digest_recipe type. Open: FEED_STATUSES "degraded" has no producer in §5.2.
- test_arming.py forge_cache helper defect fixed (param renamed `service` so the `arming=None` override works).

## F9 breaker/arming/coordination BUILT (323 passed after the forge_cache fix). Decisions:
- state.py gained SeriesState.last_trip() (folded in _fold_trip; in the snapshot payload; one test added) — the breaker reads it instead of scanning the ledger.
- HeadBoundCache(path, member, head_check) lives in breaker.py, shared by arming.py (graduate to ledger.py at review); each module passes its own _head_check so validate_cache_head stays patchable per module.
- Breaker: trip() cause ∈ {trip, halt} via CAUSE_TARGETS (-> halted); reduce/flatten/reset require control_request_id + principal_digest + proof_digest; record ids "trip:<request_id>" or "trip:<reason>:<actor>:<next_seq>", "cancel_outcome:<trip_id>"; reset reads state.last_trip(); DEFAULT_CANCEL_OPEN = True; halt with working orders but executor=None -> failed; empty acks -> unknown; already-halted trips are policy-refused (loop checks current() before re-tripping on the sentinel); cancel_outcome(acks) public for recovery's re-issue (R6).
- Arming: authority_id = canonical_hash(["ordinary-arm-v1", request_digest, request_id, approval_id]); ArmApproval.canonical_bytes() = canonical {"request_digest"}; overlay equal to the document bound accepted; empty allowlist refused; disarm(view, request_id=None)/revoke(view, reason, request_id=None) bodies omit arming; check_conjunction: rung agreement -> _RUNG_GATES table -> flag/env/release -> origin (reduction=None raises; reduction.digest cross-checked against signed). CONJUNCTION_REASONS (9) and SCOPE_REASONS closed sets live in arming.py pending vocab.py (bookkeeping: move to vocab + pin).
- Coordination: ProcessLease tokens one counter across scopes; renew reuses the acquire ttl; LeaseRenewer.tick catches any Exception from renew -> invalidate, never raise.
- Review debt for base.py: public check_digest (64-hex), public pin_members (4 spellings), the cls(params) _Configured idiom (now 5 copies). apply_scope maps size measures via document.SIZE_MEASURES -> should read Measure.scalable_field once guards lands.

## F8 guards BUILT (352 passed; the agent was cut by a rate limit after finishing — no report; read guards.py docstrings for its readings: working order with limit None valued at the proposal's reference_price when the instrument matches, else missing evidence -> refuse; infeasible amendment -> refuse).
## Rate-limit cut (12:30 UTC reset) killed F12 feed/decider, F15 monitors, T7 control/accounting, T13 reconcile/readiness before they wrote anything — relaunched. main merged again (decision-log only).

## T7 control/accounting tests DONE (90 + 89). Pinned:
- ControlInbox(serve_root, clock): surface exactly queue/pending/mark_applied/mark_rejected/receipt; no ledger; queued_at_ms inbox-assigned (caller-supplied refuses); command {request_id, purpose ∈ CONTROL_PURPOSES (= APPROVAL_PURPOSES + halt/disarm/reconcile/ready; vocab gap — one home), payload, payload_digest, release_hash, proof (bytes, base64 in file)}; control.EXECUTING_PURPOSES == ("execute_flatten",); receipt written before the inbox unlink so pending() skips any request with a receipt.
- CommandProcessor(inbox, ledger, state, handlers, clock): surface exactly process_pending(view) -> tuple[command_result body]; per command: control_request -> handler records -> command_result -> barrier -> file move; ids f"{kind}:{request_id}"; control_request.principal_digest None on receipt, proof_digest 64-hex, proof bytes never reach a record.
- Accounting ABC: three abstract hooks (value, classify, snapshot) + concrete source_tokens(executor, at_ms) -> (executor_token, accounting_tokens) (base (None, None); absence/regression/reuse-with-changed-economics enforced by the BASE); PaperAccounting(params, *, clock, history, max_valuation_age_ms) with history.fills/cash_flows/marks(since_ms) yielding §6 bodies + envelope id (supersedes names a record) — supplier: LedgerHistory(ledger) in reconcile.py; a quote past max_valuation_age_ms (inclusive) -> value() None but snapshot() REFUSES; only the four _EvidenceMeasure kinds (pnl, drawdown, consecutive_losses, error_vs_realised) are evidenced (exposure/bankroll_fraction read the fold); pnl flat->flat pinned; AccountState.positions/working/balances are the fold's (state_view); RecordedAccounting tape (method, args, answer) with args value=(at_ms, quote_digest), classify=(proposal.id,), snapshot=(at_ms, (requirement_digests,)).
- Plan gaps: history seam (§5.7.1); token seam; freshness split; formulas for drawdown/consecutive_losses/error_vs_realised unstated (implementer documents; plan edit).

## T13 reconcile/readiness tests DONE (95 + 57; 31/31 mutations caught). Pinned:
- reconcile.py: Reconciler(document, release, *, ledger, state, clock); run(view, executor, scope) -> ReconReport; sides(view, executor, scope) -> (ours, theirs); apply_policy(report); due(now_ms, last_run_ms=None) = the ONE owner of on_start/every_s (loop calls it); last_report; adopt(view, break_ids, control_request_id, principal_digest, proof_digest, release_hash, flow_kind, external) -> tuple[str]. Constants RECON_DOMAINS=(balances, fills, orders, positions, settlements), ORDER_FIELDS, ORDER_BREAK_CLASS, BREAK_SEVERITY_BY_CLASS, BREAK_ID_TAG, CASH_FLOW_ID_TAG, RECON_ID_TAG. ReconReport fields = §6's six; to_obj() IS the record body. Break 9 fields frozen (delta str|None). ours["balances"] = expected balance (fold + all-time fill cash effect; buy debits qty*price+fee); lookback_ms bounds venue queries + compared fills/settlements; fills(since, cursor) -> (page, next_cursor) paged to exhaustion (G14 executor must match); run refuses executor.execution_scope() != scope. Ours-settlements empty in phase 1 -> venue settlements are external `settlement` breaks (warn). adoption.after_recon_id None (no producer — plan edit: drop). cash_flow id = f"cash_flow:{H('cash-flow-v1', release_hash, control_request_id, break_id)}"; adoption:{control_request_id}.
- LedgerHistory(ledger): fills(since_ms) -> tuple[Fill]; cash_flows(since_ms) -> tuple[dict]; marks(since_ms) -> tuple[dict] (outcome_kind == "marked"); inclusive >= on ts_ms/effective_at_ms.
- readiness.py: Readiness(document, release, *, ledger, state, clock, checklist_path); evaluate(at_ms); record(result) -> seq; current(view, at_ms); verdict_for(view, at_ms) -> READINESS_VERDICTS (the one owner of expired => no_go); readiness_digest(release_hash, items) = canonical_hash((release_hash, tuple-of-5-tuples)); ReadinessResult 5 fields; UNWAIVABLE_ITEMS (6) in readiness.py (vocab gap); DOCUMENT_WAIVER; ITEM_FIELDS; checklist = JSON list of four-key items; current returns None when the digest is not this release's; expiry inclusive.

## F15 monitors BUILT (188 passed). Decisions:
- Loop: per appended tick/decision body: monitor.observe(body); verdict = monitor.verdict() (memoised); monitor.should_trip() ≡ status == "alarm" (only under response: halt); §6 monitor record body = {"monitor": monitor.name, **verdict.to_obj()}; monitor.response exposes the declared response; phase-1 verdicts slice="all", provisional=False.
- Compose: MONITOR_KINDS.resolve(site["uses"])(site["params"], name=key); nested window/threshold {"kind": ...} via CHUNKER_KINDS/THRESHOLD_KINDS resolved INSIDE the monitor; reference one {uses, params} or a list via REFERENCE_KINDS built as cls(params, field=<monitor field>); thresholds cls(params, benchmark=<critical_value|None>); compose supplies fit data via monitor.fit(records) (primes references).
- Snapshot: monitor.state() -> {"observations", "references", "threshold" (+ "stream")} JSON-able; restore(state) on a same-configured instance reproduces the next verdict; the loop puts {name: monitor.state()} into the snapshot (state.py seam still open — G18 pins how).
- Constants DEFAULT_MIN_N=30, DEFAULT_RESPONSE="log" (breach status log/warn -> warn, halt -> alarm), DEFAULT_PERCENTILE=0.95, DEFAULT_BINS=10, DEFAULT_DELTA=0.005, DEFAULT_STEP=1, DEFAULT_TIME_FIELD, PSI_EPSILON=1e-4, ALL_SLICE. Readings: Snapshot.sample() reconstructs bin-midpoint samples; period window = the open bucket (decision-based monitors on period refuse); DecisionRate reports a COUNT per window. Review debt: 4th copy of _Configured; _check_number local bounds rule.

## F12 feed/decider BUILT (199 passed; R12 prefix on ReleaseReader applied). Decisions:
- __main__ plan: run_doc = load_document(f"{run_dir}/config.json") -> served = serving_document(run_doc, run_dir, list(doc.serving.heads), dict(doc.serving.replay or {})) (serving_hash = served.hash) -> plan(served, serving_registry(registry)) (registers recorded-gate/recorded-stat-test owned=True) -> contract = resolved[entry].cls.serving_contract(params, evidence) -> digest, version = active_source_identity(root.registry(), contract.source_binding["source"]) -> feed_spec = FeedSpec.from_contract(contract, universe, digest, version).to_obj(); source_config = {"hash": digest, "version": version}; manifest artifact names are per FILE (artifacts/<key>/<relpath>, posix) via decider.artifact_entries(run_dir, key) (one owner for plan); evidence artifact_pinned = any manifest name under artifact_prefix(key).
- Startup: Decider(doc, release, registry=None, adapter=doc.serving.adapter, proposer=PROPOSER_KINDS.resolve(uses)(params), clock=clock); effects = decider.prepare(asof, run_dir); feed = FEED_KINDS.resolve(doc.feed.uses)(doc.feed.params, root=OnboardingRoot(contract.source_binding["root"]), registry=root.registry(), contract=decider.contract, spec=decider.feed_spec, clock=clock, max_staleness_ms=..., dead_after_ms=...).
- Tick: feed.pull(tick_at_ms) (ProductionError on alias drift BEFORE fetch; dead on OSError/ValueError) -> batch = decider.read_entry(tick_at_ms) (calls snapshot_entry) -> loop validates batch -> head_outputs, head_digest = decider.evaluate(batch) -> decider.proposer.candidates/quotes/proposals(head_outputs, candidates, account_state, Provenance(...)); ReplayFeed: pull() then batch() replaces read_entry. Proposal defaults: expires_ms = provenance.inputs_asof_ms, UNMAPPED_MONEY = 0, ratios 0.0, DEFAULT_TIF = "ioc"; DEFAULT_MAX_ARTIFACT_AGE = "P30D" in decider.py.
- GAP [safety-critical, for the G12 review R3 to FIX]: Decider.prepare does not compare release.serving_hash/run_hash/doc_hash because conftest's manifest carries placeholders. Fix: conftest binds the real hashes (serving_document is available now) and prepare refuses on mismatch (D24 "startup re-verifies all hashes"); serve also asserts release.serving_hash == decider.serving_hash.
- Review debt: private driver names imported (_is_summary, _winner_names — export in §9.1); _read_json restated; cls(params) idiom copied twice more.
