# Research

Use the `record-research` skill to prepare research. Write the final record
through `python -m dskit.journal research "TITLE" --body-file <draft>` so it
is journaled; do not create research Markdown here by hand.
